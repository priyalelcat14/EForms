<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<VBox>
		<Table id="tableBefore" inset="false" items="{path: 'approverEditClientModel>/eFormFiMaintSet'}">
			<headerToolbar>
				<Toolbar>
					<Title text="{i18n>beforeTableTitle}" level="H2"/>
					<ToolbarSpacer/>
				</Toolbar>
			</headerToolbar>
			<columns>
				<Column>
					<Text text="{i18n>LOB_Label}"/>
				</Column>
				<Column>
					<Text text="{i18n>Subob_Label}"/>
				</Column>
				<Column >
					<Text text="{i18n>Subob_Description_Label}"/>
				</Column>
				<Column>
					<Text text="FA"/>
				</Column>
				<!--<Column>
        <Text text="Level"/>
      </Column>-->
				<Column>
					<Text text="User Name"/>
				</Column>
				<Column>
					<Text text="User Id"/>
				</Column>
			</columns>
			<items>
				<ColumnListItem>
					<cells>
						<Text text="{approverEditClientModel>LOB}"/>
						<Text text="{approverEditClientModel>SUBLOB}"/>
						<Text text="{approverEditClientModel>SLOB_DESCRIPTION}"/>
						<Text text="{approverEditClientModel>FA}"/>
						<!--<Text text=""/>-->
						<Text text="{approverEditClientModel>UNAME1}"/>
						<Text text="{approverEditClientModel>UNAME}"/>
					</cells>
				</ColumnListItem>
			</items>
		</Table>
	</VBox>
</core:FragmentDefinition>